name: Variables Print

on:
  workflow_dispatch:

env:
  LOCAL_ENV: Ahoj
  NICE_VARIABLE: ${{ vars.NICE_VARIABLE }}
  NICE_VARIABLE_2: ${{ env.NICE_VARIABLE }}

jobs:
  run-tests:
    name: 'Test'
    environment: test
    runs-on: [self-hosted, nuc-runner]
    steps:
      - name: Print
        run: | 
          echo "Hodnota env 1: ${{ vars.NICE_VARIABLE }}"
          echo "Hodnota env 2: ${{ env.NICE_VARIABLE }}"
          echo "Hodnota secret 1: ${{ secrets.NICE_SECRET }}"
          echo "Local 1: $LOCAL_ENV"
          echo "Local 2: $NICE_VARIABLE"
          echo "Local 3: $NICE_VARIABLE_2"
