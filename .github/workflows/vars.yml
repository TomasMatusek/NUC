name: Variables Print

on:
  workflow_dispatch:

env:
  LOCAL_ENV: Ahoj
  NICE_VARIABLE: ${{ vars.NICE_VARIABLE }}

jobs:
  run-tests:
    name: 'Test'
    runs-on: [self-hosted, nuc-runner]
    steps:
      - name: Print
        run: | 
          echo "Hodnota env 1: ${{ vars.NICE_VARIABLE }}"
          echo "Hodnota env 2: ${{ env.NICE_VARIABLE }}"
          echo "Local: $LOCAL_ENV"
